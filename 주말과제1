#include <stdio.h>

int main() {
   char word[50];
   scanf("%[^\n]s", &word);
   int i = 0;
   int cnt = 0;
   for (i = 0; i < strlen(word); i++) {
      if (word[i] >= 97 && word[i] <= 122) {
         cnt++;
      }
   }
   printf("%d", cnt);
   return 0;
}
